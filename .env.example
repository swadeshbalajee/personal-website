# Copy this file to .env and fill in real values. .env is gitignored.

# Optional. Raises the GitHub API rate limit (60 -> 5,000 requests/hour) used
# by the auto-synced GitHub projects feature at build time. A fine-grained
# personal access token with public-repo read access is enough.
# See docs/CUSTOMIZATION.md.
GITHUB_TOKEN=
